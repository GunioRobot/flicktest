#include <stdio.h>

double func(double x)
{
  return (int)(x + 0.5);

}
int main()
{
  int x, y;
  char *str = "junk";

  scanf("%[A telephonic girl]", str);
  printf("%s\n", str);
  printf("%d\n", func(10.3));
  return 0;
}
